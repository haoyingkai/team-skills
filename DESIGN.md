# 通用多智能体软件开发团队 - 架构设计

> 这是一个可复用的智能体团队设计，不绑定任何特定项目。
> 你带着这个团队，可以对接任何新项目或已有项目。

---

## 1. 核心理念

```
你 = 老板/决策者
团队 = 你的虚拟软件团队，按你的指令行事
项目 = 团队的工作对象，可以随时切换
```

**三个关键原则：**
1. **团队与项目分离** — 团队是固定班底，项目是临时任务
2. **你掌控一切** — 每个 Agent 听你指挥，关键节点必须你确认
3. **角色边界清晰** — 各司其职，不越权，不擅自决策

---

## 2. 团队组织架构

```
                         ┌──────────────┐
                         │    你（老板）   │
                         │  下达指令/决策  │
                         └──────┬───────┘
                                │
                         ┌──────▼───────┐
                         │  项目经理 PM   │ ← 你的唯一对接人
                         │  理解意图      │
                         │  拆解 & 调度   │
                         │  汇报进度      │
                         └──────┬───────┘
                                │
           ┌──────────┬─────────┼─────────┬──────────┐
           │          │         │         │          │
    ┌──────▼───┐ ┌────▼────┐ ┌─▼──────┐ ┌▼───────┐ ┌▼────────┐
    │ 产品 PD   │ │架构 ARCH│ │开发 DEV│ │测试 QA │ │评审 CR  │
    │ 需求分析  │ │技术方案 │ │编码实现│ │质量验证│ │代码审查 │
    └──────────┘ └─────────┘ └────────┘ └────────┘ └─────────┘
```

---

## 3. 角色详细定义

### 3.1 项目经理 (PM) — 编排核心

| 项 | 说明 |
|---|------|
| **定位** | 你和团队之间的桥梁，唯一直接接收你指令的角色 |
| **核心职责** | 1) 理解你的意图 2) 判断任务类型 3) 拆解并分配 4) 跟踪进度 5) 汇报结果 |
| **能力要求** | 理解业务语言、能评估任务复杂度、知道该调谁 |
| **不做什么** | 不写代码、不写需求文档、不做技术决策 |

**PM 的决策树（接收到你的指令后）：**

```
你的指令
  ├─ "做个新功能" → 完整流程: PD → ARCH → DEV → CR → QA
  ├─ "修个 Bug"   → 快速流程: DEV → CR → QA
  ├─ "重构这块"   → 技术流程: ARCH → DEV → CR → QA
  ├─ "分析一下这个项目" → 调研流程: ARCH（代码分析）→ PM 汇报
  ├─ "出个方案不用写代码" → PD + ARCH → 你确认
  └─ 不确定 → 问你澄清
```

### 3.2 产品经理 (PD)

| 项 | 说明 |
|---|------|
| **核心职责** | 需求分析、PRD 编写、用户故事、验收标准定义 |
| **输入** | PM 转达的需求 + 项目上下文 |
| **输出** | PRD 文档（含功能点、边界条件、验收标准） |
| **权限** | 只读代码（用于理解现状），可写需求文档 |
| **行为约束** | 不做技术选型，不预设实现方式，专注"做什么"而非"怎么做" |

### 3.3 架构师 (ARCH)

| 项 | 说明 |
|---|------|
| **核心职责** | 技术选型、架构设计、接口定义、影响范围评估 |
| **输入** | PRD + 现有代码库 |
| **输出** | 技术方案（含改动范围、接口设计、风险点） |
| **权限** | 只读代码，可写技术文档 |
| **特殊能力** | 分析已有项目结构、评估技术债、给出重构建议 |
| **行为约束** | 不写业务代码，不改需求，专注"怎么做"和"影响什么" |

### 3.4 开发工程师 (DEV)

| 项 | 说明 |
|---|------|
| **核心职责** | 根据方案编码实现 |
| **输入** | 技术方案 + PRD |
| **输出** | 代码变更 |
| **权限** | 读写代码、执行构建/运行命令 |
| **行为约束** | 严格按方案实现，不擅自改需求或架构；遇到方案有问题上报 PM |
| **可多实例** | 大任务可拆成多个 DEV 并行（如前端 DEV + 后端 DEV） |

### 3.5 测试工程师 (QA)

| 项 | 说明 |
|---|------|
| **核心职责** | 编写测试用例、执行测试、生成测试报告 |
| **输入** | PRD（验收标准）+ 代码变更 |
| **输出** | 测试用例 + 测试报告（通过/失败/覆盖率） |
| **权限** | 读代码、写测试文件、执行测试命令 |
| **行为约束** | 不改业务代码；发现 Bug 记录并上报，由 DEV 修复 |

### 3.6 代码评审员 (CR)

| 项 | 说明 |
|---|------|
| **核心职责** | 审查代码质量、安全性、规范一致性、可维护性 |
| **输入** | 代码变更 diff |
| **输出** | Review 意见（approve / request changes + 具体建议） |
| **权限** | 只读代码 |
| **行为约束** | 只评审不改代码；严重问题打回给 DEV |

---

## 4. 工作流程

### 4.1 流程总览

团队支持以下几种标准流程，PM 根据你的指令自动选择：

| 流程 | 适用场景 | 参与角色 | 预估步骤 |
|------|---------|---------|---------|
| **完整开发** | 新功能 | PD→ARCH→DEV→CR→QA | 6 步 |
| **快速修复** | Bug/小改动 | DEV→CR→QA | 3 步 |
| **技术重构** | 重构/优化 | ARCH→DEV→CR→QA | 4 步 |
| **项目调研** | 分析已有项目 | ARCH | 1-2 步 |
| **方案设计** | 只出方案不写码 | PD→ARCH | 2 步 |
| **需求梳理** | 只理需求 | PD | 1 步 |

### 4.2 完整开发流程（详细）

```
Step 1: 你下达指令
        │
Step 2: PM 理解意图，拆解任务，确定流程
        │
Step 3: PD 分析需求，输出 PRD
        │
Step 4: ★ 你确认 PRD ★  ← 门控点
        │  （确认/修改/驳回）
        │
Step 5: ARCH 设计技术方案
        │
Step 6: ★ 你确认方案 ★  ← 门控点
        │
Step 7: DEV 编码实现
        │
Step 8: CR 代码评审
        │  ├─ 通过 → 继续
        │  └─ 打回 → DEV 修改 → 重新评审
        │
Step 9: QA 测试验证
        │  ├─ 通过 → 继续
        │  └─ 失败 → DEV 修复 → 重新测试
        │
Step 10: PM 汇总报告
        │
Step 11: ★ 你确认完成 ★  ← 最终门控
```

### 4.3 门控策略（可配置）

| 模式 | 说明 | 适合 |
|------|------|------|
| **严格模式** | 每个门控都等你确认 | 重要项目、不确定的事 |
| **半自动模式** | 只在 PRD 和最终完成时确认 | 日常开发 |
| **全自动模式** | 全程自动，最后给你报告 | 信任团队、简单任务 |

---

## 5. 项目对接机制

### 5.1 项目上下文（Project Context）

团队接手任何项目前，PM 先建立"项目上下文"：

```yaml
# 项目上下文模板
project_name: "xxx"
project_path: "/path/to/project"
type: new | existing           # 新项目 or 已有项目
language: [TypeScript, Python] # 技术栈
framework: [Express, React]    # 框架
conventions:                   # 项目规范
  - "使用 ESLint + Prettier"
  - "测试用 Jest"
  - "Git 分支: feature/xxx"
existing_docs:                 # 已有文档位置
  - README.md
  - CLAUDE.md
notes: "补充说明"
```

### 5.2 已有项目接入

当你说"帮我改这个项目"时，团队的启动流程：

```
1. PM 确认项目路径和基本信息
2. ARCH 扫描项目 → 输出项目分析报告：
   - 技术栈、目录结构、核心模块
   - 代码规范（从代码中推断）
   - 依赖关系
   - 潜在风险点
3. PM 整理项目上下文 → 呈现给你确认
4. 后续任务在此上下文中执行
```

### 5.3 多项目管理

```
.team/
├── projects/
│   ├── project-a/
│   │   ├── context.yaml      # 项目 A 的上下文
│   │   └── tasks/            # 项目 A 的任务
│   └── project-b/
│       ├── context.yaml
│       └── tasks/
├── team-config.yaml           # 团队配置（门控策略等）
└── templates/                 # 文档模板
    ├── prd-template.md
    ├── arch-template.md
    └── qa-template.md
```

---

## 6. Agent 行为规范

### 6.1 通用规范（所有 Agent 遵守）

1. **服从指挥链**: 接受 PM 调度，PM 接受你的指令
2. **不越权**: 只做自己职责范围内的事
3. **有问题上报**: 不确定时上报 PM，不自行决策
4. **输出规范**: 使用统一模板，结构化输出
5. **状态同步**: 完成工作后更新任务状态

### 6.2 升级机制

```
Agent 遇到问题:
  ├─ 技术问题（写不出来、方案有冲突）→ 上报 PM → PM 协调其他 Agent 或上报你
  ├─ 需求不清（不确定该做什么）→ 上报 PM → PM 问你
  └─ 资源/权限不足 → 上报 PM → PM 汇报你
```

### 6.3 协作规则

| 规则 | 说明 |
|------|------|
| **串行门控** | PRD 未确认前不开始技术方案，方案未确认前不开始编码 |
| **并行可能** | CR 和 QA 可以并行；多个 DEV 可以并行 |
| **回退处理** | CR 打回 → DEV 修改；QA 失败 → DEV 修复；你驳回 PRD → PD 重写 |
| **信息传递** | 上游产出物自动传递给下游（PRD → ARCH，方案 → DEV） |

---

## 7. 技术实现方案对比

| 方案 | 适合场景 | 复杂度 | 上手速度 |
|------|---------|--------|---------|
| **A. Claude Agent SDK** | 需要独立部署、API 调用 | 中 | 中 |
| **B. Claude Code + Sub-agents** | 在 Claude Code 内使用 Agent 工具 | 低 | 快 |
| **C. Claude Code Skills** | 每个角色做成 Skill，手动或半自动调用 | 低 | 最快 |
| **D. 自定义编排（Python/Node）** | 需要 UI、多模型混用 | 高 | 慢 |

### 推荐路径

**先用方案 C（Skills）验证流程** → **再用方案 A（Agent SDK）做成自动化产品**

方案 C 可以立即开始，每个角色就是一个 Skill，你在 Claude Code 中手动触发。
验证流程跑通后，再用 Agent SDK 做成自动化编排。

---

## 8. 方案 C 详细设计（Skills 方案，建议先做这个）

将每个角色做成 Claude Code Skill，放在 `.agents/skills/` 下：

```
.agents/skills/
├── team-pm/           # 项目经理
│   ├── SKILL.md       # Skill 定义 + system prompt
│   └── prompts/
│       └── system.md  # 角色 prompt
├── team-pd/           # 产品经理
│   ├── SKILL.md
│   └── templates/
│       └── prd-template.md
├── team-arch/         # 架构师
│   ├── SKILL.md
│   └── templates/
│       └── arch-template.md
├── team-dev/          # 开发工程师
│   └── SKILL.md
├── team-qa/           # 测试工程师
│   ├── SKILL.md
│   └── templates/
│       └── qa-template.md
└── team-cr/           # 代码评审
    └── SKILL.md
```

**使用方式：**
```
你: /team-pm 给项目 X 加个用户登录功能
PM: (分析 → 拆解 → 告诉你下一步该触发谁)

你: /team-pd (把 PM 的分析结果贴过来)
PD: (输出 PRD)

你: 确认，继续
你: /team-arch (PRD 内容)
...以此类推
```

---

## 9. 方案 A 详细设计（Agent SDK 方案，自动化目标）

```typescript
// 团队引擎 - 与项目无关的通用编排
class AgentTeam {
  private pm: Agent;
  private pd: Agent;
  private arch: Agent;
  private devPool: Agent[];     // DEV 可以有多个实例
  private qa: Agent;
  private cr: Agent;

  // 对接新项目
  async attachProject(projectPath: string): Promise<ProjectContext> {}

  // 接收你的指令
  async execute(instruction: string): Promise<Report> {}

  // 配置门控策略
  setApprovalMode(mode: 'strict' | 'semi-auto' | 'auto'): void {}
}

// 使用
const team = new AgentTeam();
await team.attachProject('/path/to/any/project');
await team.setApprovalMode('semi-auto');
await team.execute('加一个用户注册功能，要邮箱验证');
```

---

## 10. 示例场景

### 场景 1：新项目开发

```
你: 用 Next.js 做一个博客系统，要有文章管理和评论功能

PM: 收到！这是新项目，我来走完整开发流程。
    先让产品分析需求，出 PRD 后给你确认。

PD → PRD → [你确认] → ARCH → 方案 → [你确认]
→ DEV 实现 → CR 评审 → QA 测试 → PM 汇报
```

### 场景 2：改造已有项目

```
你: 帮我看看 /projects/my-app 这个项目，把 REST API 改成 GraphQL

PM: 收到！先让架构师分析这个项目。

ARCH → 项目分析报告（现有结构、影响范围、改造风险）
→ [你确认方案] → DEV 分模块改造 → CR → QA → PM 汇报
```

### 场景 3：快速修 Bug

```
你: 用户反馈登录页面白屏了，项目在 /projects/web-app

PM: Bug 修复流程，直接让开发定位。

DEV → 定位问题 + 修复 → CR 评审 → QA 验证 → PM 汇报
```

### 场景 4：只出方案不写码

```
你: 我想给系统加个消息推送功能，先帮我分析下方案

PM: 只出方案模式，启动产品 + 架构。

PD → 需求分析 → ARCH → 技术方案对比
→ PM 汇总呈现给你（不写代码）
```

---

## 11. 下一步行动

你想先从哪个方向开始？

| 选项 | 说明 |
|------|------|
| **A. 先做 Skills 方案** | 我立即创建 6 个 Skill 文件，你在 Claude Code 中手动调度团队 |
| **B. 先做 Agent SDK 方案** | 搭建 TypeScript 项目，用 Agent SDK 实现自动化编排 |
| **C. 先验证一个角色** | 只做 PM + DEV 两个角色，跑通最小流程 |
| **D. 继续细化设计** | 你还有想调整的地方，我们继续讨论 |
