# AI 开发团队 — 架构设计文档

> 版本: 2.0.0 | 更新日期: 2026-04-10
> 基于 Claude Code Skills 实现的多智能体软件开发团队

---

## 1. 核心理念

```
你 = 老板/决策者
团队 = 你的虚拟软件团队，按你的指令行事
项目 = 团队的工作对象，可以随时切换
```

**三个关键原则：**
1. **团队与项目分离** — 团队是固定班底（通过 Git 仓库分发），项目是临时任务
2. **你掌控一切** — 每个角色听你指挥，关键节点必须你确认
3. **角色边界清晰** — 各司其职，不越权，不擅自决策

---

## 2. 团队组织架构

```
                         ┌──────────────┐
                         │    你（老板）   │
                         │  下达指令/决策  │
                         └──────┬───────┘
                                │
                         ┌──────▼───────┐
                         │  项目经理 PM   │ ← 你的唯一对接人（永远在线）
                         │  老周          │
                         │  理解意图      │
                         │  拆解 & 调度   │
                         │  汇报进度      │
                         └──────┬───────┘
                                │
           ┌──────────┬─────────┼─────────┬──────────┐
           │          │         │         │          │
    ┌──────▼───┐ ┌────▼────┐ ┌─▼──────┐ ┌▼───────┐ ┌▼────────┐
    │ 产品 PD   │ │架构 ARCH│ │开发 DEV│ │测试 QA │ │评审 CR  │
    │ 小苏      │ │老陈     │ │阿杰    │ │小林    │ │老吴     │
    │ 需求分析  │ │技术方案 │ │编码实现│ │质量验证│ │代码审查 │
    └──────────┘ └─────────┘ └────────┘ └────────┘ └─────────┘
```

辅助模块：
- **team-board** — 任务看板（已整合到 PM）
- **team-team-up** — Skill 研发架构师（不随仓库分发，仅开发者本地使用）
- **team-tmp** — 权限设置助手（不随仓库分发，仅开发者本地使用）

---

## 3. 技术实现

### 3.1 实现方案

采用 **Claude Code Skills** 方案。每个角色是一个独立 Skill，由 SKILL.md 定义角色的人设、职责、工作流和行为约束。复杂角色配有 `references/` 子目录存放手册和模板。

### 3.2 Skill 文件结构

```
skills/
├── team-pm/                    # 项目经理 老周
│   ├── SKILL.md                # 角色定义
│   └── references/
│       ├── handbook.md         # PM 工作手册（详细流程）
│       ├── protocol.md         # 团队协作协议（全角色共用）
│       └── templates/
│           ├── prd-template.md
│           ├── arch-template.md
│           └── qa-template.md
├── team-pd/                    # 产品经理 小苏
│   ├── SKILL.md
│   └── references/
│       └── handbook.md
├── team-arch/                  # 架构师 老陈
│   ├── SKILL.md
│   └── references/
│       └── handbook.md
├── team-dev/                   # 开发工程师 阿杰
│   ├── SKILL.md
│   └── references/
│       └── handbook.md
├── team-qa/                    # 测试工程师 小林
│   ├── SKILL.md
│   └── references/
│       └── handbook.md
├── team-cr/                    # 代码评审 老吴
│   ├── SKILL.md
│   └── references/
│       └── handbook.md
└── team-board/                 # 任务看板（整合到 PM）
    └── SKILL.md
```

### 3.3 角色间依赖关系

- **所有角色**引用 `../team-pm/references/protocol.md`（团队协作协议）
- **PD** 引用 `../team-pm/references/templates/prd-template.md`（PRD 模板）
- **ARCH** 引用 `../team-pm/references/templates/arch-template.md`（技术方案模板）
- **QA** 引用 `../team-pm/references/templates/qa-template.md`（测试报告模板）
- 依赖通过**相对路径**（`../team-pm/`）引用，要求所有 skill 安装在同一父目录下

### 3.4 运行时数据

团队运行时产生的任务数据存放在项目的 `.team/` 目录下（不在仓库中）：

```
.team/
├── tasks/
│   └── TASK-001/
│       ├── task.json           # 任务元数据
│       ├── prd.md              # PD 产出的需求文档
│       ├── arch.md             # ARCH 产出的技术方案
│       ├── review.md           # CR 产出的评审意见
│       └── qa-report.md        # QA 产出的测试报告
└── lessons.md                  # 经验库
```

---

## 4. 角色详细定义

### 4.1 项目经理 (PM) — 老周

| 项 | 说明 |
|---|------|
| **定位** | 团队入口，永远在线，唯一直接接收老板指令的角色 |
| **核心职责** | 理解意图、拆解任务、调度角色、跟踪进度、汇报结果 |
| **工作模式** | 老周只跟老板对话，所有后台操作委托助理（Agent 子代理）异步执行 |
| **不做什么** | 不写代码、不写需求文档、不做技术决策 |

**PM 的决策树：**

```
老板的指令
  ├─ "做个新功能" → 完整流程: PD → ARCH → DEV → CR → QA
  ├─ "修个 Bug"   → 快速流程: DEV → CR → QA
  ├─ "重构这块"   → 技术流程: ARCH → DEV → CR → QA
  ├─ "分析一下这个项目" → 调研流程: ARCH → PM 汇报
  ├─ "出个方案不用写代码" → PD + ARCH → 老板确认
  └─ 不确定 → 问老板澄清
```

### 4.2 产品经理 (PD) — 小苏

| 项 | 说明 |
|---|------|
| **核心职责** | 需求分析、交互式需求澄清、竞品调研、PRD 编写、验收标准定义、E2E 产品验收 |
| **输入** | PM 转达的需求 + 项目上下文 |
| **输出** | PRD 文档（含功能点、边界条件、验收标准）、HTML 原型截图 |
| **行为约束** | 不做技术选型，不预设实现方式，专注"做什么"而非"怎么做" |

### 4.3 架构师 (ARCH) — 老陈

| 项 | 说明 |
|---|------|
| **核心职责** | 技术选型、架构设计、接口定义、已有项目分析、改动影响评估、开源方案调研 |
| **输入** | PRD + 现有代码库 |
| **输出** | 技术方案（含改动范围、接口设计、风险点） |
| **特殊能力** | 支持搜索开源项目和技术方案作为架构参考 |
| **行为约束** | 不写业务代码，不改需求，专注"怎么做"和"影响什么" |

### 4.4 开发工程师 (DEV) — 阿杰

| 项 | 说明 |
|---|------|
| **核心职责** | 根据技术方案编码实现，遵循项目规范 |
| **输入** | 技术方案 + PRD |
| **输出** | 代码变更 |
| **行为约束** | 严格按方案实现，不擅自改需求或架构；遇到方案有问题上报 PM |

### 4.5 测试工程师 (QA) — 小林

| 项 | 说明 |
|---|------|
| **核心职责** | 编写测试用例、执行测试、生成测试报告 |
| **输入** | PRD（验收标准）+ 代码变更 |
| **输出** | 测试用例 + 测试报告（通过/失败/覆盖率） |
| **行为约束** | 不改业务代码；发现 Bug 记录并上报 |

### 4.6 代码评审员 (CR) — 老吴

| 项 | 说明 |
|---|------|
| **核心职责** | 审查代码质量、安全性、规范一致性、可维护性 |
| **输入** | 代码变更 diff |
| **输出** | 评审意见（approve / request changes + 具体建议） |
| **行为约束** | 只评审不改代码；严重问题打回给 DEV |

---

## 5. 工作流程

### 5.1 流程总览

| 流程 | 适用场景 | 参与角色 | 
|------|---------|---------|
| **完整开发** | 新功能 | PD→ARCH→DEV→CR→QA |
| **快速修复** | Bug/小改动 | DEV→CR→QA |
| **技术重构** | 重构/优化 | ARCH→DEV→CR→QA |
| **项目调研** | 分析已有项目 | ARCH |
| **方案设计** | 只出方案不写码 | PD→ARCH |
| **需求梳理** | 只理需求 | PD |

### 5.2 完整开发流程

```
Step 1: 老板下达指令
        │
Step 2: PM 理解意图，拆解任务，确定流程
        │
Step 3: PD 分析需求，输出 PRD
        │
Step 4: ★ 老板确认 PRD ★  ← 门控点
        │
Step 5: ARCH 设计技术方案
        │
Step 6: ★ 老板确认方案 ★  ← 门控点
        │
Step 7: DEV 编码实现
        │
Step 8: CR 代码评审
        │  ├─ 通过 → 继续
        │  └─ 打回 → DEV 修改 → 重新评审
        │
Step 9: QA 测试验证
        │  ├─ 通过 → 继续
        │  └─ 失败 → DEV 修复 → 重新测试
        │
Step 10: PM 汇总报告 → ★ 老板确认完成 ★
```

### 5.3 协作规则

| 规则 | 说明 |
|------|------|
| **串行门控** | PRD 未确认前不开始技术方案，方案未确认前不开始编码 |
| **并行可能** | CR 和 QA 可以并行 |
| **回退处理** | CR 打回 → DEV 修改；QA 失败 → DEV 修复；老板驳回 PRD → PD 重写 |
| **信息传递** | 上游产出物自动传递给下游 |

---

## 6. 分发架构

### 6.1 Git 仓库

```
ai-dev-team/                    ← Git 仓库根目录
├── .git/                       # Git 版本管理
├── skills/                     # Skill 文件（唯一真相源）
│   ├── team-pm/
│   ├── team-pd/
│   ├── team-arch/
│   ├── team-dev/
│   ├── team-qa/
│   ├── team-cr/
│   └── team-board/
├── CHANGELOG.md                # 迭代记录
├── DESIGN.md                   # 本文档
└── README.md                   # 安装使用指南
```

### 6.2 远程仓库

- **地址**: `https://gitee.com/hao_yk/hyk-skills.git`
- **分支策略**:
  - `main` — 正式版，仅在老板明确说"发布"时才合并
  - `dev` — 开发测试版，每次修改后自动同步

### 6.3 安装方式

通过 `npx skills`（Vercel 出品的 Skill 包管理器）安装：

```bash
# 同事安装正式版（从远程 main 分支）
npx skills add https://gitee.com/hao_yk/hyk-skills.git

# 只安装特定角色
npx skills add https://gitee.com/hao_yk/hyk-skills.git --skill team-pm

# 开发者安装本地 dev 版（用于测试）
npx skills add /Users/haoyk/claudeSkilltry/ai-dev-team --agent claude-code -y
```

### 6.4 Gitee 兼容性说明

- Gitee URL **必须加 `.git` 后缀**
- `--ref` 和 `#branch` 对 Gitee **无效**，远程安装始终拉默认分支（main）
- 本地路径安装读取当前 checkout 的分支
- 结论：远程安装 = 正式版，本地安装 = 开发版，天然隔离

### 6.5 多工具兼容

`npx skills` 支持 45+ AI 工具，同一份 SKILL.md 可安装到：
- Claude Code（`.claude/skills/`）
- Cursor（`.cursor/skills/`）
- Codex、Gemini CLI、GitHub Copilot（`.agents/skills/`）
- Windsurf、Kiro 等

---

## 7. 迭代管理

### 7.1 迭代角色

团队 Skill 的修改由 **team-team-up**（Skill 研发架构师）统一管理：
- 是团队体系的**唯一修改入口**
- 不随仓库分发，仅在开发者本地使用
- 每次修改后自动同步 dev 分支

### 7.2 迭代流程

```
需求/反馈 → team-team-up 评估影响 → 修改 Git 仓库中的 skill 文件
→ 更新 CHANGELOG.md + DESIGN.md → commit + push dev → 汇报
→（老板说"发布"时）merge dev → main，打 tag
```

### 7.3 版本号规则

- **主版本** (x.0.0): 新增/删除角色、重大流程变更
- **次版本** (0.x.0): 角色职责调整、新增能力、流程优化
- **补丁版本** (0.0.x): 文案修正、格式统一、小 bug 修复

---

## 8. 使用方式

在 Claude Code 中直接喊名字或使用 Skill 命令：

```
你: 老周，帮我做个用户登录功能
你: 小苏，讨论下需求
你: 老陈，做个技术方案
你: 阿杰，开始开发
你: 小林，跑一下测试
你: 老吴，review 一下代码
```

**路由规则**：
1. **老周永远在线**：没有明确指向某个角色的消息，默认老周响应
2. **指名找人**：消息中出现名字，调用对应角色
3. **按内容路由**：触发词匹配到特定角色时自动调度
